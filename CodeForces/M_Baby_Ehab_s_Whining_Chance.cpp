#include <bits/stdc++.h>
using namespace std;
typedef long long ll;
#define INGZzz  	cin.tie(0);cin.sync_with_stdio(0);
const int N = 1e5;

int main()
{
    INGZzz
    freopen("lis.in", "r", stdin);
    string s;
    cin >> s;
    if(s.size() == 1) cout << s;
    else if(s.size())  
    
    return 0;
}